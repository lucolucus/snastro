package snastro.kernel

/** Identity of the `Parlante` aggregate: a UUID v4 string minted through [GeneratoreId] (Published Language). */
@JvmInline
public value class ParlanteId(public val valore: String)
