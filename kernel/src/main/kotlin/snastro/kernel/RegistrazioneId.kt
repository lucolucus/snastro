package snastro.kernel

/** Identity of the `Registrazione` aggregate: a UUID v4 string minted through [GeneratoreId] (Published Language). */
@JvmInline
public value class RegistrazioneId(public val valore: String)
