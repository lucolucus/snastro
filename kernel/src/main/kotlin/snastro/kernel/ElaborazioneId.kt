package snastro.kernel

/** Identity of the `Elaborazione` aggregate: a UUID v4 string minted through [GeneratoreId] (Published Language). */
@JvmInline
public value class ElaborazioneId(public val valore: String)
