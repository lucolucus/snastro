package snastro.parlanti.applicazione.eventi

/**
 * Published Language projection of `TipoParlante` (boundary `eventi-parlanti`), so no consumer ever
 * sees the Parlanti domain enum.
 */
public enum class TipoParlanteVista { RICORRENTE, OCCASIONALE }
