package snastro.ui

import snastro.kernel.RegistrazioneId

/** `tec-shell-ui`: what changed, published by [AggiornamentiVista] (R15). `null` = everything may have changed. */
data class Cambiamento(val registrazioneId: RegistrazioneId?)
