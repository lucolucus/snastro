# Context map — snastro

> Project-level strategic trunk. AMENDED by every feature, never re-forked.
> Canonical terms are Italian (profile `ubiquitous_language.lang: it`). Each term is written as
> `Term` (schema/type name) = meaning. **One concept = one name.** The "Not:" lists name the
> synonyms that must NOT appear in code (verifier grep).

## Bounded context: Progetto
- **Role:** supporting — upstream (owns the project and its catalogue of recordings; scopes everything else)
- **Ubiquitous language:**
  - `Progetto` = a container the user opens and keeps feeding over time; it scopes the recordings AND the gallery of `Parlante`s (identities never cross projects). Not: workspace, cartella, sessione.
  - `Registrazione` = one audio file the user already recorded, added to a `Progetto` (source file + `titolo` + date of the recording + duration). `titolo` = the source file name without extension, fixed at import and **immutable** (user decision 2026-09-23); **unique per `Progetto`** — on a clash (compared on its file-safe, case-insensitive form) `AggiungiRegistrazione` appends " (2)", " (3)"… (user decision 2026-09-23), so the `Documento` file name is unique. It is the unit of processing and of output (one `Documento` per `Registrazione`). Not: audio, file, meeting, riunione, sessione, clip.
  - `DataRegistrazione` = the date the recording took place (defaults to the file's date, user-editable); used to label `Parlante`s of type `occasionale` ("ospite del 12/09"). Not: data import.
  - `Eliminare una Registrazione` / "Elimina…" (`EliminaRegistrazione` → `RegistrazioneEliminata`) *(added 2026-09-25 [user], [ADR 0020](decisions/0020-elimina-registrazione.md))*. This is the user's **hard** delete of one `Registrazione` from its `Progetto`, and it cannot be undone. It removes:
    - the audio copied into the project;
    - every `Elaborazione` and the `Trascritto`;
    - the `Documento`;
    - every `Attribuzione` and `ImprontaVocale` of its `Voce`s.

    `ricorrente` `Parlante`s stay, with their prints from other recordings. An `occasionale` seen only there ceases to exist (INV-25). It is refused while an `Elaborazione` is `in_attesa` or `in_corso`: annullare it first, or wait until it ends. It is distinct from `Eliminazione del Parlante`, which leaves a tombstone. The original file outside the project is never touched. Not: cancellare, rimuovere, archiviare, nascondere (as distinct terms); scartare (reserved for the internal compensation of a failed import).
- **Notes:** audio decoding/normalization (format conversion, resampling) is a technical adapter inside `Trascrizione`, NOT a context and NOT part of `Registrazione`'s language.
- **Introduced by:** trascrizione-con-parlanti

## Bounded context: Trascrizione
- **Role:** core — upstream of `Parlanti` and `Documento`; downstream of `Progetto`
- **Ubiquitous language:**
  - `Elaborazione` = one run of the local pipeline on a `Registrazione` (diarization + speech-to-text), long-running, with a `StatoElaborazione`. ~~v1: at most ONE completed `Elaborazione` per `Registrazione` — re-running it on a reviewed recording is not offered (retry only after `fallita`).~~ *(amended 2026-09-24 [user], [ADR 0018](decisions/0018-ritrascrivi.md))* Several may be `completata` (history); the `Trascritto` is the result of the latest one. A new one can start whenever none is open: "Trascrivi", "Riprova" after `fallita`, "Ritrascrivi" after `completata`. An `Elaborazione` still `in_attesa` (never started) can be annullata (see `Annullare`). Not: job, task, processing, analisi.
  - `StatoElaborazione` = `in_attesa | in_corso | completata | fallita`.
  - `Ritrascrivere` / "Ritrascrivi" *(added 2026-09-24 [user], ADR 0018)* = starting a new `Elaborazione` on a `Registrazione` that already has a `Trascritto`. The old `Trascritto` stays (read-only in S3) until the new run completes, and is then replaced whole: its `Voce`s are renumbered from 1 and the names given to them (`Attribuzione`s, prints) are lost. If the run fails or is annullata, nothing changes. Not: rielaborare, rifare, reset.
  - `Annullare` (una `Elaborazione` in coda) *(added 2026-09-24 [user], ADR 0018 Amendment (b))* = withdrawing an `Elaborazione` that is still `in_attesa` (never started): it disappears and the `Registrazione` returns to its previous state (no `Elaborazione`, or its old `Trascritto`). A running (`in_corso`) `Elaborazione` cannot be annullata. Not: interrompere, fermare, abortire, cancellare.
  - `Numero di persone` (`NumeroPersone`) = how many people speak in a `Registrazione`, as stated by the user when starting an `Elaborazione` ("Trascrivi", or "Riprova" prefilled from the failed run). It is **optional**; when stated, it is an integer from **1 to 10**. It is carried by `AvviaElaborazione`, stored immutable on the `Elaborazione` it was given for, and passed to the `Diarizzatore` as the exact number of `Voce`s to form. When absent, clustering is automatic. It is a hint to diarization, NOT a count of `Parlante`s or of `Voce`s (the result may have fewer `Voce`s). User decision 2026-09-23, ADR 0014. Not: numero parlanti, numero speaker, num_clusters, partecipanti.
  - `FaseElaborazione` = the pipeline stage an `in_corso` `Elaborazione` is in: `decodifica | diarizzazione | trascrizione | allineamento` (displayed as "preparazione audio", "separazione voci", "trascrizione", "allineamento"). Progress information only, NOT guarded state: no invariant depends on it (INV-3 on `StatoElaborazione` unchanged). Not: step, stadio, progresso, avanzamento, passo.
  - `Trascritto` = the structured transcript of one `Registrazione`: the ordered `Segmento`s and the `Voce`s they belong to. **Source of truth** for the text (the `.md` is derived). Not: trascrizione (that is the context name), transcript, testo.
  - `Voce` = a distinct voice detected by diarization inside ONE `Registrazione` (a cluster), labelled `Voce 1`, `Voce 2`… until attributed. It has no identity across recordings — that is `Parlante`'s job. Not: cluster, gruppo di voce, speaker, parlante anonimo, locutore.
  - `Segmento` = a time interval (start, end) of the `Registrazione` with its recognized text, belonging to exactly one `Voce`. Mixed IT/EN text in a single `Segmento` is normal (code-switching). Not: turno, battuta, utterance, frase, chunk.
  - `Segmento confermato` *(added 2026-09-24 [user], [ADR 0019](decisions/0019-separazione-semi-automatica.md))* = a `Segmento` the user has placed or confirmed on its `Voce` by an explicit act (a manual move, a division, "Dai un nome a una frase"). It is never moved automatically. It carries no name. Not: frase bloccata, fissata, verificata.
  - `Revisione` = the user's correction of diarization errors on a `Trascritto`: **unire** two `Voce`s, **dividere** a `Voce` into two, **riassegnare** a `Segmento` to another `Voce`. Words of the text are NOT editable in v1. Not: correzione, editing, modifica.
- **Notes:** the correction of *who a `Voce` is* (a wrong name) is NOT `Revisione` — it lives in `Parlanti` (`Attribuzione`). `Revisione` changes the structure (which segments belong together); `Attribuzione` changes the identity.
- **Introduced by:** trascrizione-con-parlanti

## Bounded context: Parlanti
- **Role:** core (the differentiator: persistent identity per `Progetto` across recordings) — downstream of `Trascrizione` and `Progetto`; upstream of `Documento`
- **Ubiquitous language:**
  - `Parlante` = a real person's identity inside ONE `Progetto`, persistent across its `Registrazione`s; has a `Nome` and a `TipoParlante`. Not: persona, speaker, identità, partecipante, utente.
  - `Nome` = the display name of a `Parlante`, **unique among the `attivo` `Parlante`s of a `Progetto`**. Naming is optional: a `Voce` the user skips becomes an `occasionale` with the provisional `Nome` "Ospite del <DataRegistrazione>". Renaming changes only the displayed name everywhere (past `Documento`s regenerated), never the past diarization. Not: etichetta, alias, label.
  - `TipoParlante` = `ricorrente | occasionale`. `occasionale` = a one-off guest whose `ImprontaVocale`s ARE kept and who can be proposed later and **promosso** to `ricorrente`. Behavioural difference: label, the provisional "Ospite del …" `Nome` for unnamed `occasionale`s, and `ricorrente` `Candidato`s ranked first in a `Proposta`. An `occasionale` left with no `Attribuzione` (e.g. after a re-attribution or a `Revisione`) ceases to exist — distinct from `Eliminazione del Parlante`: no tombstone (tactical INV-25). Not: ospite/fisso/guest/one-off as type names.
  - `StatoParlante` = `attivo | eliminato`. An `eliminato` `Parlante` is a **name-only tombstone**: it keeps its `Nome` (past `Trascritto`s/`Documento`s still show it) but has no `ImprontaVocale`, is never a `Candidato`, receives no new `Attribuzione` (sole exception: in `unire` the tombstone `Attribuzione` of the removed `Voce` is re-keyed onto the surviving one — tactical INV-21, ADR 0012 Amendment (b)), and its `Nome` becomes reusable by a new `Parlante`. Not: archiviato, disattivato, anonimizzato.
  - `ImprontaVocale` = a voice-print (embedding) of a `Parlante`, extracted from one attributed `Voce` (from a bounded selection of its `Segmento`s — `SorgenteImpronta`) of one `Registrazione`. A `Parlante` keeps SEVERAL (one per contributing `Voce`), never a single average. Biometric data of third parties, stored locally only. Not: voiceprint, embedding, firma vocale, profilo vocale.
  - `Galleria` = the set of all `ImprontaVocale`s of a `Progetto`'s `Parlante`s, against which a new `Voce` is compared. Not: archivio, database voci, libreria.
  - `Proposta` = for one `Voce` of a new `Registrazione`, the ranked list (`ricorrente` first, then by `Fascia`) of `Candidato`s from the `Galleria`, shown with a `Fascia` and an `EstrattoAudio` — never a percentage. Not: suggerimento, match, riconoscimento, proposta di identità.
  - `Candidato` = one `Parlante` inside a `Proposta`, with its `Fascia`.
  - `Fascia` = `forte | debole | nessuna` — the similarity band of a `Candidato`, from calibrated `SoglieFascia`. Not: punteggio, confidenza, percentuale, score.
  - `SoglieFascia` = the calibrated thresholds that turn similarity into a `Fascia` (output of spike `impronta-vocale-affidabilita`). Not: soglia di match.
  - `EstrattoAudio` = the ~10 s of audio of a `Voce` the user listens to before deciding: its longest `Segmento`s (≥ 1 s each, at most 3, ≤ 10 s total) concatenated (user decision 2026-09-23). For a `Candidato`, built the same way from the `Voce` that sourced one of its `ImprontaVocale`s. Not: snippet, anteprima, campione.
  - `Attribuzione` = the user-confirmed link `Voce` (of a `Registrazione`) → `Parlante`. Skipping a `Voce` counts as confirming it as a new `occasionale` ("Ospite del …"). Only confirmed `Attribuzione`s feed new `ImprontaVocale`s. Not: assegnazione, associazione, mapping, etichettatura.
  - `Eliminazione del Parlante` = privacy right: purges ALL the `Parlante`'s `ImprontaVocale`s (biometric data) from the `Galleria` and sets `StatoParlante = eliminato`; the `Nome` and past `Attribuzione`s survive, so past `Documento`s keep showing the name. Not: rimozione, cancellazione (as distinct terms).
  - `Frase di riferimento` *(added 2026-09-24 [user], ADR 0019 + Amendment (b))* = a `Segmento` of ≥ 1 s used as the voice sample of a `Parlante` in one `Registrazione`: its `Segmento confermato`s on the `Voce`s attributed to that person or, if there are none, every `Segmento` of those `Voce`s ("tutta la voce"). Derived, never stored. Not: campione, esempio, frase modello.
  - `Riassegna per somiglianza` *(added 2026-09-24 [user], ADR 0019 + Amendment (b))* = the S3 action that compares every movable `Segmento` with each person's frasi di riferimento, shows a **preview** of the moves, and on "Applica" puts each one confidently similar to a person on that person's `Voce`. Nothing moves before "Applica". Not: auto-assegna, riconoscimento automatico, riclassifica.
  - `frase incerta` *(added 2026-09-24 [user], ADR 0019)* = a `Segmento` that "Riassegna per somiglianza" cannot place with confidence (too short, too similar to two people, or unlike everyone). It **stays where it is**. Not: frase dubbia, scartata.
  - `Proposta di unione` (`PropostaUnione`) = when two `Voce`s of the SAME `Registrazione` are attributed to the same `Parlante`, the app proposes (one click, never automatic, never blocking) to **unire** them via `Revisione`. Not: merge automatico, conflitto.
- **Introduced by:** trascrizione-con-parlanti

## Bounded context: Documento
- **Role:** supporting — downstream of `Trascrizione` and `Parlanti` (pure projection, owns no source of truth)
- **Ubiquitous language:**
  - `Documento` = the `.md` file of ONE `Registrazione` ("who says what"), **regenerated** from `Trascritto` + `Attribuzione`s + `Nome`s; never edited as a source, never parsed back. Not: export, file md, report, verbale.
  - `Rigenerazione` = rewriting a `Documento` after its inputs change (end of `Elaborazione`, a `Revisione`, a new/changed `Attribuzione`, a `Parlante` renamed/deleted/promosso). Not: aggiornamento, riesportazione.
- **Introduced by:** trascrizione-con-parlanti

## Bounded context: Sintesi
- **Role:** supporting — downstream of `Trascrizione`, `Parlanti` and `Progetto`; upstream of nothing (read only by the UI). It owns the `Riassunto`s and nothing else: it never re-models the transcript (it reads `Segmento`s through a consumer-owned port, like `Documento`), never writes the `.md`, never commands another context.
- **Ubiquitous language:** *(added 2026-09-25 by feature `sintesi` [user decisions + challenger RESHAPE])*
  - `Riassunto` = the result of summarizing ONE `Registrazione` with the local LLM, shown in the "Riassunto" tab of the recording page. It holds a `Sommario`, and lists of `Decisione`s, `QuestioneAperta`s, `Azione`s and `PuntoChiave`s, each with its `Fonte`s. At most ONE `Riassunto` is shown per `Registrazione`: the latest `pronto`. It is **never** part of the `Documento`, and the `Documento` never depends on it. Not: sintesi (that is the context name), summary, verbale, report, resoconto, minuta.
  - `StatoRiassunto` = `in_attesa | in_corso | pronto | fallito`. A new `Riassunto` starts `in_attesa`. While it runs, the previous `pronto` one stays shown; a `pronto` one **replaces** the previous whole; `fallito` changes nothing that was shown. Not: job state, stato elaborazione.
  - `Riassumere` / "Riassumi" = the request for a new `Riassunto` of a `Registrazione` that has a `Trascritto`, with an optional `Argomento`. It is made by the user, with ONE automatic exception: after a `Ritrascrivere` completes, a `Registrazione` that had a `Riassunto` gets a new "Riassumi" enqueued automatically with the same `Argomento` (user decision 2026-09-25). Nothing else re-summarizes on its own: not a first `Elaborazione`, not a `superato` `Riassunto`. Not: sintetizza, genera riassunto, rigenera, aggiorna.
  - `Argomento` = the short, optional text the user gives at "Riassumi" to say *di cosa si parla*. It drives the exclusion of what is `fuori tema`. It belongs to the `Riassunto` it was given for (kept with it, prefilled at the next "Riassumi"). Without an `Argomento` nothing is excluded. Not: contesto (that is a DDD word here), tema, prompt, descrizione, oggetto.
  - `fuori tema` = the part of a `Registrazione` unrelated to the `Argomento` (chit-chat, another project). It is left out of the `Riassunto`: it never appears as a `Decisione`, `Azione` or `PuntoChiave`. Not: off-topic, rumore, divagazione.
  - `Sommario` = the short prose (a few sentences) at the top of a `Riassunto`. A speaker it mentions (like one mentioned in the text of any element) is stored as a `Voce` reference and rendered with the current `Nome`, never as a literal name (user decision 2026-09-25). Not: riassunto breve, abstract, sintesi.
  - `Decisione` = something the speakers **agreed** on, with its `Fonte`s. Something only proposed, or left "da valutare", is NOT a `Decisione`. Not: delibera, conclusione, accordo.
  - `QuestioneAperta` = something proposed or discussed but **not agreed** ("da valutare", "ci pensiamo"), with its `Fonte`s. Kept apart from `Decisione` on purpose (challenger RESHAPE (4)). Not: proposta (that is a `Parlanti` term), da valutare (as a type name), ipotesi, pendenza.
  - `Azione` = something someone has to do after the `Registrazione`, with its `Fonte`s and an optional `Responsabile`. Not: task, todo, compito, cosa da fare (the UI label "Cose da fare" is allowed as text only), action item.
  - `Responsabile` = the `Voce` of the `Registrazione` who will carry out an `Azione`, or none. It is kept as a reference to the `Voce`, never as a name, so it shows the **current** `Nome` of whoever that `Voce` is attributed to ("Voce n" while unattributed). Not: assegnatario, owner, incaricato.
  - `PuntoChiave` = a relevant point of the discussion, possibly bound to who said it, with its `Fonte`s. Not: highlight, punto saliente, takeaway.
  - `Fonte` = the id of a `Segmento` of the `Trascritto` the `Riassunto` was made from, cited as the ground of an element. It is shown as the speaker (the `Nome` of the `Segmento`'s `Voce`, resolved at display) + the minute. Not: citazione, riferimento (taken by "Frase di riferimento"), source, prova.
  - `Verifica delle fonti` = the check applied to every LLM answer before a `Riassunto` becomes `pronto`: each `Fonte` must be an existing `Segmento` of that `Trascritto`, and a speaker the element binds (a `PuntoChiave`'s speaker, a `Responsabile`) must be a `Voce` of that `Trascritto` — for a `PuntoChiave`, one of the `Voce`s of its `Fonte`s. An invalid `Fonte` is **dropped**; an element left with **no valid `Fonte`** is dropped too and counted, **never shown as fact** (challenger RESHAPE (2)). Not: validazione, grounding, controllo citazioni.
  - `superato` (`Riassunto superato`) = a `pronto` `Riassunto` whose `Trascritto` has changed **in structure** through a `Revisione` since it was made (unire, dividere, riassegnare — including "Riassegna per somiglianza"). It stays readable with a notice and a manual "Riassumi di nuovo"; it is **never regenerated automatically**. Renaming a `Parlante`, a new or changed `Attribuzione`, `Eliminazione del Parlante`, promotion, and confirming a `Segmento` do **NOT** make it `superato`: speakers are `Voce` references resolved to the current `Nome` at display (challenger RESHAPE (1), confirmed by the user 2026-09-25). Not: obsoleto, scaduto, stale, non aggiornato, invalidato.
  - `Riassunto` and `Ritrascrivere` *(user decision 2026-09-25)* = a `Ritrascrivere` is NOT a `superato` case. When the new `Trascritto` replaces the old one, the `Riassunto` of that `Registrazione` is **deleted**, because its `Fonte`s and `Voce` references would point at a new generation, i.e. at the wrong people. A new "Riassumi" with the same `Argomento` is then enqueued automatically. A `Ritrascrivere` that fails or is annullata changes nothing.
- **Constraints (user decisions 2026-09-25):**
  - Local LLM only, **bundled in the app** (no user-installed Ollama, no cloud): Qwen3.5 9B q4_K_M (Apache-2.0, 6.6 GB), provisioned through `:modelli` like every other model (research `features/sintesi/research/scelta-modello-llm.md`). The runtime (llama.cpp JNI vs `llama-server` sidecar) is spike `runtime-llm-in-app`.
  - Budget: a `Riassunto` of 1 h of audio (≈ 25k tokens) in **≤ ~5 min** on the M3 Pro (measured 175 s). Above ≈ 1 h 15 (the 32k context) "Riassumi" is refused with a plain message until spike `contesto-lungo` closes.
  - A `Riassunto` never runs at the same time as an `Elaborazione`: summarizing and transcription share ONE serial execution (the `:avvio` queue), for memory and for the ADR 0011 time budget (challenger RESHAPE (3)).
  - Everything a `Riassunto` holds is meeting content. It is stored in the **`Progetto`'s SQLite database** like every other entity (ADR 0006/0010), not as separate files (user decision 2026-09-25: it is backed up with the project, it can be searched later, and there are fewer files). It is never sent anywhere.
  - The LLM model is **optional**: onboarding (S5) is unchanged, and the model is downloaded the first time the user asks "Riassumi" (user decision 2026-09-25).
  - `Elaborazione`s and `Riassunto`s share ONE **FIFO** queue, served in request order (user decision 2026-09-25).
  - Only the current `Riassunto` is kept: there is no history of replaced ones, and a queued `Riassunto` cannot be cancelled in the first release (user decision 2026-09-25).
  - Deleting a `Registrazione` (ADR 0020, pending on the sibling branch) deletes its `Riassunto`, queued ones included, for privacy. See Relationships `Progetto` → `Sintesi`.
  - Numbering reserved for `Sintesi`: ADRs from **0021**, and project-DB migration **`6.sqm`**.
- **Out of scope (later releases, not modeled):** chapters of a `Registrazione`; the `Progetto`-level summary (across `Registrazione`s); clicking a `Fonte` to play the audio (the `Fonte` already carries what it needs); editing a `Riassunto`; putting it into the `Documento` (never); questions/chat over the transcript; **full-text search / indexing over `Riassunto`s and `Trascritto`s**. Search is a known LATER consumer, so the storage schema must not preclude it: the text is kept as queryable columns in the project DB, not as opaque blobs.
- **Introduced by:** sintesi *(placeholder "Future context (v2)" of trascrizione-con-parlanti replaced)*

## Relationships
- `Progetto` → `Trascrizione` : upstream/downstream, Customer/Supplier — `Trascrizione` reads a `Registrazione` (identity + source audio) via a port; never writes the catalogue.
- `Progetto` → `Parlanti` : upstream/downstream — every `Parlante` and the `Galleria` are scoped by `Progetto` identity (no cross-project matching).
- *(amended 2026-09-25, [ADR 0020](decisions/0020-elimina-registrazione.md))* `Progetto` publishes `RegistrazioneEliminata`, and `Trascrizione` (veto + purge), `Parlanti` (purge + INV-25) and `Documento` (file removal, after commit) react to it. `Progetto` still never depends on them.
- `Trascrizione` → `Parlanti` : upstream/downstream, Customer/Supplier — `Parlanti` reads the `Voce`s of a completed `Trascritto` and their `Segmento` intervals (to extract `ImprontaVocale`s / `EstrattoAudio` and build `Proposta`s); it reacts to `Revisione` (voci unite/divise, segmento riassegnato) to keep `Attribuzione`s and `ImprontaVocale`s consistent. `Trascrizione` never knows names.
- `Trascrizione` → `Documento` : upstream/downstream, conformist — `Documento` renders the `Trascritto` as-is.
- `Parlanti` → `Documento` : upstream/downstream, conformist — `Documento` resolves each `Voce` to a `Nome` via `Attribuzione` (every `Voce` gets an `Attribuzione` — a skipped one becomes an `occasionale` "Ospite del …"; `Voce n` only while identification is not done yet; an `eliminato` `Parlante` still resolves to its `Nome`). The `Documento` is generated even if the user skips naming.
- `Trascrizione` → `Sintesi` : upstream/downstream, Customer/Supplier *(amended 2026-09-25, feature `sintesi`; was "future downstream consumer, not modeled")* — `Sintesi` reads, through its OWN consumer-owned port (same shape as `Documento`'s `LettoreTrascritto`/`SegmentoVista`: `segmentoId`, `voceId`, interval, text), the `Segmento`s of the current `Trascritto`, to build the LLM input, to run the `Verifica delle fonti`, and to tell when a `Riassunto` is `superato`. It also reads whether a `Trascritto` exists and whether a re-run is open ("Riassumi" is offered only when a `Trascritto` exists and no `Elaborazione` of that `Registrazione` is open). It **subscribes** to the published `TrascrittoSostituito` (ADR 0018): it deletes the `Riassunto` of that `Registrazione` and, once the replacing `Elaborazione` is committed, enqueues a new "Riassumi" with the same `Argomento` (user decision 2026-09-25). `Sintesi` never writes or commands `Trascrizione`; `Trascrizione` does not know `Sintesi` exists (it only publishes).
- `Parlanti` → `Sintesi` : upstream/downstream, conformist — `Sintesi` resolves each `Voce` (of a `Fonte`, of a `Responsabile`, of a `PuntoChiave`) to the current `Nome` through its OWN port (same shape as `Documento`'s `LettoreNomi`: `VoceRef` → `Nome`), at display time and to label the LLM input. A `Riassunto` stores no `Nome` as its reference and no `ParlanteId`.
- `Progetto` → `Sintesi` : upstream/downstream — every `Riassunto` belongs to one `Registrazione` and is stored in its `Progetto`'s database. `Sintesi` **subscribes** to `RegistrazioneEliminata(registrazioneId, progettoId, titolo, dataRegistrazione, riferimentoAudio)` published by `Progetto` (ADR 0020 "Elimina registrazione", **pending on the sibling branch** `feature/trascrizione-con-parlanti`) and deletes every `Riassunto` of that `Registrazione`, including a queued one, which must then never run (privacy). A `Riassunto` completing on a deleted `Registrazione` writes nothing.
- `Documento` ⟂ `Sintesi` : **separate ways** — no relationship in either direction. The `Documento` is generated with or without any `Riassunto` and never waits for the LLM.
- Execution (technical, not a domain relationship): the `:avvio` serial queue runs `Elaborazione`s and `Riassunto`s one at a time; `Sintesi` does not enter `Trascrizione`'s aggregates to use it.
- Single side (`app`): every boundary is `in-process` (port + contract test), no OpenAPI.

## Open spikes (unknowns/risks → future spike nodes)
> Stack context (ADR 0001/0004/0011): all-Kotlin app, ML in-process via sherpa-onnx (ONNX models
> only; pyannote community-1, Sortformer and MLX dropped), NFR = 1 h of audio in ≤ 10 min
> end-to-end on the M3 Pro. Authoritative detail: `features/trascrizione-con-parlanti/tasks/app/backlog/<id>.md`.
- [ ] impronta-vocale-affidabilita: **PARTIALLY answered 2026-09-24 by [ADR 0019](decisions/0019-separazione-semi-automatica.md) §2 — the embedding MODEL is chosen: NeMo TitaNet-small (`embedding-nemo-titanet-small`, shared with the diarizer; catalogue entry in ADR 0019 §1.7); `estrattore-impronta-sherpa` is unblocked. STILL OPEN: `SoglieFascia` calibration, false-"forte" rate / go-no-go for automatic `Proposta`s, `BUDGET_IMPRONTA_MS`, the ≥ 3-recording cross-session measurement.** on the user's REAL recordings, does `ImprontaVocale` similarity (sherpa `SpeakerEmbeddingExtractor` + cosine in Kotlin) separate same-person from different-person across devices/rooms/sessions well enough to produce useful `Fascia`s? Embedding model: first reuse the diarizer's extractor, then sherpa catalogue alternatives (3D-Speaker ERes2Net/V2/CAM++, WeSpeaker ResNet34/ECAPA, NeMo TitaNet; VoxCeleb preferred). — Closes when: a same/different-person similarity distribution is measured on ≥ 3 real `Registrazione`s with ≥ 2 recurring people, `SoglieFascia` (forte/debole) are calibrated, the false "forte" rate is reported (go/no-go for automatic `Proposta`s), extraction time is counted in the NFR budget, `BUDGET_IMPRONTA_MS` (print source duration, provisional 30 s) is calibrated (error at 10/20/30/60 s; concatenation vs per-`Segmento` averaging), and model + thresholds are recorded in an ADR. — expected side: app — owner: trascrizione-con-parlanti
- [x] scelta-diarizzatore: **closed 2026-09-23 by [ADR 0014](decisions/0014-diarizzazione-sherpa-numero-persone.md) [user]** *(configuration superseded 2026-09-24 by [ADR 0019](decisions/0019-separazione-semi-automatica.md) §1: seg-3.0 fp32 + TitaNet-small + own AHC clustering; the semi-automatic "Riassegna per somiglianza" repairs what clustering cannot separate)* — pyannote-3.0 int8 + WeSpeaker ResNet34-LM, threshold 0.4, wsr 0.5; optional "Numero di persone" → `num_clusters` (auto otherwise); closed on user acceptance with 2 of ≥ 3 samples and no DER (re-measure on a 3rd recording). Original question: which sherpa-onnx `OfflineSpeakerDiarization` configuration (segmentation: pyannote segmentation-3.0 ONNX vs reverb-diarization-v1 if its licence allows; embedding extractor from the sherpa catalogue; FastClustering threshold) produces the `Voce`s for IT/EN meetings with 2–4 speakers within the NFR? — Closes when: run on ≥ 3 real samples with error estimate (DER or wrong turns / 10 min) + wall-clock per hour on the M3 Pro + licence per model recorded, and one configuration is chosen in an ADR (`:modelli` catalogue entry). — expected side: app — owner: trascrizione-con-parlanti
- [x] scelta-asr-code-switching: **closed 2026-09-23 by [ADR 0013](decisions/0013-asr-parakeet.md) [user]** — Parakeet TDT 0.6B v3 int8 (Qwen3-ASR measured and rejected; Whisper turbo not measured, not needed); closed on user acceptance with 2 of ≥ 3 samples, no real English turns, no WER (re-measure on a 3rd recording). Original question: Parakeet TDT 0.6B v3 int8 vs Whisper large-v3-turbo ONNX (sherpa `OfflineRecognizer`) for Italian+English with in-sentence code-switching, measured against the NFR (1 h ≤ 10 min on the M3 Pro). — Closes when: both run on real mixed IT/EN samples with scored accuracy on switched phrases + timestamp availability + wall-clock per hour + total pipeline time vs the 600 s budget recorded, and one is chosen in an ADR (if only Whisper is acceptable and misses the budget, the trade-off goes back to the user). — expected side: app — owner: trascrizione-con-parlanti
- [x] allineamento-parole-voci: **closed 2026-09-24 by [ADR 0015](decisions/0015-allineamento-per-turno.md) [user]** — strategy (A), transcribe per diarization turn (merge same-voice turns with a gap < 1 s; turns < 500 ms skipped; turns > 25 s split by Silero VAD; empty text dropped; one `Segmento` per merged turn; overlaps kept); strategy (B) rejected, not measured; closed on user acceptance with 2 recordings and no scored WER (re-measure on a 3rd recording). Original question: how are diarization turns + ASR output combined into `Segmento`s (A: transcribe per turn, default leaning; B: whole-recording VAD chunks + token timestamps assigned by midpoint), with overlapping speech KEPT (`Segmento`s may overlap, INV-7)? — Closes when: the chosen strategy produces `Segmento`s on a real sample whose attribution errors are only diarization errors (fixable by `Revisione`), overlap handling is documented, its time is counted in the NFR budget, and an ADR records it. — expected side: app — owner: trascrizione-con-parlanti
- [x] packaging-modelli-desktop: **closed 2026-09-24 by [ADR 0016](decisions/0016-packaging-nativi-sherpa.md)** — sherpa-onnx v1.13.8 from GitHub release assets (not Maven Central), pinned SHA-256; the jar is fetched for compile and the osx-arm64 JNI natives by `scaricaNativiSherpa` into `:avvio` Compose app resources; explicit `LibraryUtils.load()` with `sherpa_onnx.native.path`; all steps PASS from `./gradlew run`, the `.app` and the `.dmg` on macOS arm64; Windows/Linux documented, untested; CPU stays default (CoreML accepted, not measured). OPEN [user], not blocking R1: which JDK the `.dmg` bundles, and signing/notarization. Original question: hello-world — can a Compose Desktop app load the sherpa-onnx JNI + onnxruntime natives (pinned, SHA-256 verified), decode an `.m4a` with bytedeco FFmpeg, run VAD + one embedding extraction and play audio, both from `./gradlew run` and from a jpackage `.dmg`? — Closes when: it runs on macOS (M3 Pro) both ways, the Windows/Linux native + model-cache paths are documented, and an ADR records fetch coordinates/checksums and the jpackage resource layout. — expected side: app — owner: trascrizione-con-parlanti
- [x] attesa-mutex-estrazione: **closed 2026-09-24 by [ADR 0017](decisions/0017-attesa-mutex-estrazione.md)** — (b)+(c): one shared fair Mutex held per native call (already true for ASR/VAD in R1), wait made interruptible; only diarization (one native call, ≤ its ADR 0011 share, measured 67–245 s per 75 min) can make an extraction wait minutes, shown on the S3 card as "In attesa dell'elaborazione…" with "Annulla" after 2 s, never a frozen screen; (a) separate session rejected (no concurrent-session safety evidence). Original question: a `ConfermaAttribuzione`/`SaltaVoce`/`Proposta` during an `Elaborazione` waits (potentially minutes) for the single native Mutex (ADR 0012 (b) point 5) — separate small ONNX session for the print extractor vs pipeline releasing the lock between chunks vs a UI "occupato" state? — Closes when: an ADR picks the option (measured on the M3 Pro if it changes the Mutex contract) and states the user-visible behaviour. — expected side: app — owner: trascrizione-con-parlanti (added 2026-09-23, user decision: readiness gate on `avvio-coda-elaborazioni` + `schermata-registrazione`)
- [ ] runtime-llm-in-app: *(added 2026-09-25, feature `sintesi`)* how does the app run Qwen3.5 9B q4_K_M (GGUF) **bundled**, with no user-installed Ollama: llama.cpp in-process via JNI, or a `llama-server` sidecar process on loopback? Must give a JSON-schema/grammar-constrained answer, use the GPU (Metal) on the M3 Pro, be cancellable, release its memory after a `Riassunto`, and be packaged like sherpa (pinned natives, SHA-256, ADR 0016). The sidecar means a loopback HTTP client, which today breaks ADR 0008's `enforced_by` (network only in `:modelli`) and needs the amendment ADR 0008 reserved for "Ollama"; JNI does not. The GGUF lives on Hugging Face, not on the k2-fsa releases ADR 0008 names, and is 6.6 GB. It is optional and downloaded on the first "Riassumi" (user decision 2026-09-25), a case ADR 0008's "required models at onboarding" flow does not cover yet. — Closes when: the chosen runtime runs the model from `./gradlew run` AND from the `.dmg` on macOS arm64, on the Via Roquel input (≈ 24k tokens) within 175 s ± 20 % with schema-valid output, peak RSS and unload measured, cancellation behaviour stated, Windows/Linux natives documented (untested allowed), and an ADR records runtime + native coordinates/checksums + the `:modelli` catalogue entry (host, SHA-256, licence Apache-2.0, attribution) + any ADR 0008 amendment. — expected side: app — owner: sintesi
- [ ] contesto-lungo: *(added 2026-09-25, feature `sintesi`)* a `Registrazione` longer than ≈ 1 h 15 (> ~28k input tokens) does not fit the 32k context: split + recompose (per-part `Riassunto`s merged into one), a larger context, or both? The result must keep every `Fonte` valid (`Verifica delle fonti` unchanged), must not duplicate or lose a `Decisione` across parts, must keep `Decisione`/`QuestioneAperta` apart, and must stay linear in the budget (≤ ~5 min per hour). Until it closes, "Riassumi" is refused above the limit with a plain message. — Closes when: a strategy is run on a ≥ 2 h input (real, or two real recordings concatenated) with time, memory and a user judgement of lost/duplicated `Decisione`s recorded, and an ADR fixes the strategy and the limit. — expected side: app — owner: sintesi
- [ ] filtro-fuori-tema: *(added 2026-09-25, feature `sintesi`)* given a short `Argomento`, does the model leave out what is `fuori tema` WITHOUT losing on-topic `Decisione`s/`Azione`s — and with no `Argomento`, does it summarize everything? Not measured by the model spike. — Closes when: on ≥ 2 real recordings that contain off-topic talk, each is summarized with and without an `Argomento`; the user judges that off-topic content is left out and that no on-topic `Decisione`/`Azione` is lost (count reported); prompt wording and the `Argomento` length limit are recorded (ADR or amendment of the runtime ADR). — expected side: app — owner: sintesi
- [ ] qualita-riassunto: *(added 2026-09-25, feature `sintesi`)* is the content of a `Riassunto` right, beyond structural validity? Open from the model spike: things "da valutare" filed as `Decisione`s, the same `Fonte`s duplicated across `PuntoChiave`s and `Decisione`s, a one-sentence `Sommario` on a short recording; new: the prose and the elements must name speakers only through `Voce` tokens (rendered as the current `Nome` at display), not as literal names — does that cost quality? — Closes when: the user judges the `Riassunto`s of ≥ 2 real recordings (`Decisione`s true / missed / invented, `Decisione` vs `QuestioneAperta` correctly separated, `Responsabile`s right), the `Verifica delle fonti` drop counts are reported, and the prompt + answer schema v1 are fixed in an ADR. — expected side: app — owner: sintesi
